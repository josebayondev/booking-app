# Variables de entorno del frontend. Copia este fichero a .env y ajusta los valores.
# Solo las que empiezan por VITE_ llegan al navegador, así que aquí no va nunca un secreto.

# Base de la API del backend. En local, el uvicorn de backend/ o el docker-compose.
VITE_API_BASE_URL=http://localhost:8000
